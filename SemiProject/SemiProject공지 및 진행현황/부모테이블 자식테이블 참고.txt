부모테이블 : guest
create table guest(num smallint auto_increment primary key,
myid varchar(20),
photo varchar(20),
content varchar(2000),
writeday date)

자식테이블 :answer
create table answer(idx smallint auto_increment primary key,
num smallint,
myid varchar(20),
content varchar(1000),
writeday date,
foreign key(num) references guest(num) on delete cascade);

location.reroad